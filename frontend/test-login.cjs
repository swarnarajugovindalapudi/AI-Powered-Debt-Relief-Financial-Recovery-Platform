const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173'); await page.type('input[name="email"]', 'demo@gmail.com'); await page.type('input[name="password"]', 'demo1234'); await page.click('button[type="submit"]'); await page.waitForNetworkIdle(); const error = await page.evaluate(() => document.querySelector('.text-red-500')?.textContent || document.querySelector('.text-red-400')?.textContent || document.body.innerText); console.log('RESULT:', error); await browser.close(); })();
